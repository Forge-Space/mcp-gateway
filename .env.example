# MCP Gateway – copy to .env and set real values.
# Do not use these placeholder values in production; generate strong secrets.
# Values with # ! % $ or spaces: use single quotes (e.g. PLATFORM_ADMIN_PASSWORD='pass#word!')

HOST=0.0.0.0
PORT=4444
DATABASE_URL=sqlite:///./mcp.db

JWT_SECRET_KEY=REPLACE_WITH_STRONG_SECRET
AUTH_ENCRYPTION_SECRET=REPLACE_WITH_STRONG_SALT
PLATFORM_ADMIN_EMAIL=admin@example.com
PLATFORM_ADMIN_PASSWORD=REPLACE_ME
PLATFORM_ADMIN_FULL_NAME="Platform Administrator"

MCPGATEWAY_UI_ENABLED=true
MCPGATEWAY_ADMIN_API_ENABLED=true
SECURE_COOKIES=false
AUTH_REQUIRED=true

# Optional: ports for local translate services (defaults in docker-compose)
# SEQUENTIAL_THINKING_PORT=8013
# CHROME_DEVTOOLS_PORT=8014
# PLAYWRIGHT_PORT=8015
# MAGICUIDESIGN_PORT=8016
# DESKTOP_COMMANDER_PORT=8017
# PUPPETEER_PORT=8018
# BROWSER_TOOLS_PORT=8019
# TAVILY_PORT=8020
# FILESYSTEM_PORT=8021
# REACTBITS_PORT=8022
# SNYK_PORT=8023
# SQLITE_PORT=8024
# GITHUB_PORT=8025

# Optional: tavily translate service needs API key (tavily-mcp)
# TAVILY_API_KEY=your-tavily-api-key

# Optional: filesystem translate service – host path to expose in container
# FILESYSTEM_PATH=/workspace
# FILESYSTEM_VOLUME=./workspace

# Optional: snyk translate service (snyk mcp -t stdio --experimental)
# SNYK_TOKEN=your-snyk-api-token

# Optional: sqlite translate service – db path inside container; host dir mounted at SQLITE_VOLUME
# SQLITE_DB_PATH=/data/sqlite.db
# SQLITE_VOLUME=./data

# Optional: github translate service (@modelcontextprotocol/server-github)
# GITHUB_PERSONAL_ACCESS_TOKEN=your-github-personal-access-token

# Optional: URL for register script health check (default http://localhost:PORT). If health fails, script retries with localhost/127.0.0.1 swapped. On Docker Desktop for Mac, prefer http://localhost:PORT if 127.0.0.1 fails or hangs.
# GATEWAY_URL=http://localhost:4444

# Optional: register script waits up to N seconds for gateway /health (default 90)
# REGISTER_GATEWAY_MAX_WAIT=90

# Optional: wait N seconds after gateway is up before registering (translate containers need time to start)
# REGISTER_WAIT_SECONDS=30

# Optional: gateways to register via scripts/register-gateways.sh (comma-separated Name|URL)
# EXTRA_GATEWAYS=Context7|https://mcp.context7.com/mcp,context-awesome|https://www.context-awesome.com/api/mcp
