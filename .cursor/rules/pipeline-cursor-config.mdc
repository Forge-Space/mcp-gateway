---
description: MCP gateway setup pipeline. Apply when editing README or start script.
globs: README.md, start.sh
alwaysApply: false
---
# Setup pipeline

**Order:** Copy `.env.example` to `.env` and set secrets → Run `./start.sh` → Add gateways in Admin UI → Configure Cursor (JWT + mcp.json) → Restart Cursor.

Reference: README "Quick start" and "Connect Cursor".
